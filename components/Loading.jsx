import Loader from "@/components/Loader";

export default function Loading() {
    return (
        <Loader />
    )
    // Or a custom loading skeleton component
    // return <p className="text-4xl bg-red-500">Loading...</p>
}